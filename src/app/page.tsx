import React from 'react';
import {ProjectList} from "@/widgets/projects";

const Page = async () => {
    return (
        <div className="container mx-auto">
            <ProjectList />
        </div>
    );
};

export default Page;
